'use strict'

expect    = require 'expect.js'
Styledocs = require '../coffee/styledocs'

describe 'styledocs', ->
  beforeEach ->
    this.styledocs = new Styledocs

  describe '.getSections(path)', ->
  describe '.compile(markdown)', ->
  describe '.output(subdir, input_file, html_raw)', ->
  describe '.execute()', ->
